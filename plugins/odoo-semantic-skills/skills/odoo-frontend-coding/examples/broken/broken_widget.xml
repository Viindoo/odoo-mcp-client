<?xml version="1.0" encoding="UTF-8"?>
<!--
  Deliberately-broken sample. verify-frontend.sh must BLOCK on classes 1, 3, 6.
  Do NOT copy this into real code - it is an acceptance-test fixture.
-->
<templates xml:space="preserve">
    <t t-name="my_module.BrokenWidget">
        <Dialog title="'Edit'">
            <!-- BUG class 6: body content in a named 'body' slot Dialog never renders -->
            <t t-set-slot="body">
                <!-- BUG class 3: raw contenteditable instead of web_editor Wysiwyg -->
                <div contenteditable="true" t-ref="editor"/>
                <!-- BUG class 1: bare free-identifier arrow handler (onSave not on component) -->
                <button t-on-click="() => onSave()">Save</button>
            </t>
        </Dialog>
    </t>
</templates>
