# Compression Report - odoo-ai-agents

Semantic-preserving compression of runtime docs (agents / skills / commands / references /
snippets / docs). Branch `chore/compress-plugin-docs`. Token estimate = words / 0.75.

## Before -> After (token)

| Scope | Files in scope | Compressible | Changed | Words before -> after | Tokens before -> after | Saved |
|---|---|---|---|---|---|---|
| odoo-ai-agents | ~245 | 185 | 78 | 246,702 -> 244,896 | ~328,936 -> ~326,528 | **~2,408 tok (0.73%)** |

### Per-cluster
| Cluster | Files | Words before -> after | Status (compressed / light / skipped) |
|---|---|---|---|
| Agents (14) | 14 | 38,211 -> 37,654 (-557) | 11 / 2 / 1 |
| Eng skill bodies (11) | 11 | 41,607 -> 41,287 (-320) | 8 / 0 / 3 |
| Eng skill references (24) | 24 | 33,203 -> 32,989 (-214) | 5 / 7 / 12 |
| Other skills A + B + commands | 78 | 72,841 -> 72,408 (-433) | 9 / 26 / 43 |
| Snippets + docs | 56 | 58,993 -> 58,720 (-273) | 10 / 17 / 29 |

## What was compressed (3 layers, body only)

1. **Semantic** - verbose narration tightened to terse imperative sentences (agent prologues,
   "When to invoke", persona/round framing in dev/sales docs, phase prose in fp/rb/upg detail).
2. **Within-file dedup** - e.g. odoo-code-reviewer "an unrun gate is not a green gate" stated
   once (was 2x); odoo-coding CHP resume logic consolidated; INSTANCE-ALLOCATION B2 model stated
   once + cross-ref; odoo-intake's three "owns own Plan Mode" bullets merged.
3. **Inline-copy -> existing pointer** - restatements of already-pointer-cited SSOT shortened:
   "Report language" -> `snippets/language-mirroring.md`; standalone-fallback -> `snippets/disk-fallback-protocol.md`;
   git-delegation sentence -> `snippets/git-delegation.md`; run-harness continuation tails -> `snippets/continuation-contract.md`.

No new reference files created (the codebase is already progressively disclosed; forcing pushdown
would risk dangling links). Snippets kept self-contained (intra-file trim only).

## Skipped entirely (out of scope - NOT touched)

| Group | Files | Reason |
|---|---|---|
| `skills/_shared/coding_guidelines/<version>/**` | 56 | Verbatim upstream Odoo RST (source URL header), loaded by exact version-built path. Compressing = corrupting source-of-truth + breaking path-load. |
| `snippets/{cursor-rules,openai-gpt-instructions,gemini-gem-instructions}.md` | 3 | GENERATED by `gen_surface.py` (BEGIN/END markers). |
| `docs/reference/ORCHESTRATION-MAP.md` | 1 | GENERATED from `generator/skill_tool_deps.json`. |
| `## MCP tools` generated regions inside 27 SKILL.md | (regions) | GENERATED; prose outside markers compressed, regions untouched. |

Agent-skipped (no safe compression - already table/code/schema/invariant-dense): odoo-forward-port
SKILL, odoo-installable-prober, and ~91 files left unchanged where the only content was preserved
data (tables, JSON/YAML schemas, bash, examples, version pivots).

## Preservation - verified

- **0** frontmatter `name`/`description` (full block) changes across all 94 changed files (independent
  byte-diff vs `master`).
- **0** generated-region changes; `make gen` is idempotent (0 diff).
- **0** introduced dangling links; **0** files reverted (no self-check failures).
- Behavior gate GREEN: `make validate` + `make test` (**946 passed, 2 skipped**) + `make deps-check`
  (43 skills + 4 agents, all tool refs resolve). Includes test_skill_format,
  test_skill_description_budget, test_naming_consistency.

## Honest note on yield

Survey estimate was ~10%; realized **0.73%**. Root cause (data-driven, spot-checked): the repo is
**already heavily SSOT-factored and progressively disclosed** - snippets are referenced by path
(not copied), ETHOS loads globally (0 copies), and prose-heavy skills are mostly preserved tables /
templates / examples with thin framing prose. Per the preservation-first contract (compress wording,
never invariants; when ambiguous, compress less), aggressive trimming was correctly declined. The
realized savings are small but verified-safe.

## Pre-existing issues flagged (NOT fixed - out of scope)

- `docs/setup.md` references `docs/tools/mcp-server.md` which does not exist (pre-existing in `master`).
